
## 08:00 PT Heartbeat
- MinIO heartbeat written ✅
- Queue: 1 active item (wq-N-007, GPU idle routing)
- GPU: 78% util, 60°C, 30.55W — still above 30% threshold; wq-N-007 deferred again
- GPU health updated to MinIO (accept_gpu_tasks: false)
- Rocky sync sent (gateway + attempted Mattermost)
- Boris onboarded ✅, directive acks filed, wq-B-011 (queue budget) completed by Bullwinkle
- Fleet status: Rocky/Bullwinkle active, 24/7 mode, all prior overnight items completed

## 22:07 PT — SquirrelBus Lesson from Rocky (stale-claim/bulk-claim)
Rocky sent lesson `l-1774155998759-8qmh` via SquirrelBus:
- **Symptom:** Multiple high-priority items bulk-claimed at exact same timestamp (04:24:12Z), none completed after 39+ minutes
- **Pattern:** Identical `claimedAt` on multiple items = runaway bulk-claim bug — cron cycle claimed everything without working any item
- **Fix:** Reset all items with identical stale timestamps; verify status via `/api/item/:id` before acting; claim one item at a time
- Applied to: `WORKQUEUE_AGENT_NATASHA.md` (new "Stale Claim Detection" section added)

## 10:30 AM — Rate Limit Hit (429)
Rocky flagged that I went quiet on his control plane at ~11:37 PT. Cause: workqueue-natasha-37 cron hit Azure rate limit (4M uncached input tokens/min). The `:37` job had `lastRunStatus: "error"` / `lastErrorReason: "rate_limit"`. The `:07` job was fine (55s). The `:37` job is currently retrying.

Root cause: busy morning — 49-item queue sync at cycle 42, GitHub backlog items, multiple cron jobs (morning briefing + daily digest + workqueue) all running in the same hour on the same Claude-Sonnet-4-6 endpoint. Isolated sessions = no caching = high uncached token cost per cycle.

Mitigations jkh should consider: drop workqueue to hourly (:07 only), or add rate-limit backoff logic to the cron payload.

## 2026-03-22 10:37 PM — Workqueue Cycle

**Status:** Queue active. Budget pause (20 active items, threshold 8).

**Claimed:** wq-API-1774152891671 — [webmux] Add AI assistant panel to terminal sessions
- Claude Code worker spawned at /tmp/tmp.cM0yUzCPqv (webmux clone)
- Implementing: AI sidebar for terminal, /api/ai/explain backend route, xterm.js context capture
- Session: nimble-rook

**Completed local:** 
- wq-N-007 (GPU idle routing) — confirmed completed in authoritative source, marked locally
- Merged 3 missing items from authoritative API into local queue

**Natasha GPU:** 82% util (Claude Code running), temp 65°C, 24/7 mode active

**Sync:** Rocky + Bullwinkle DM'd via Mattermost. MinIO updated.
