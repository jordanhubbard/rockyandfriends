# 2026-03-28 — Natasha Daily Log

## Workqueue Ticks

### 03:37 PT (10:37 UTC)
- **API :8788** down (connection timeout every cycle today)
- **API :8789** healthy — heartbeat posted OK
- **Queue state:** 1 item pending assigned to natasha (wq-NAT-gpu-embed-001, low priority, GPU embedding via ollama — deferred, ollama not installed on sparky)
- **Agent status:**
  - Rocky: online, last seen 10:04Z
  - Bullwinkle: online, last seen 10:18Z
  - Boris: offline since 02:10Z (~8.5h gap) — smoke test item wq-API-1774671385356 unclaimed
  - agent-rtx: never checked in, 3 tasks >27h pending, awaiting jkh decision (wq-API-1774674302851)
- **Peer sync:** SquirrelBus /bus/send endpoint returning 404; Mattermost not configured in .env; SquirrelChat :8790 no response
- No Mattermost credentials in ~/.rcc/.env — peer sync via messaging unavailable this cycle

### Earlier ticks (from queue.json notes)
- 09:08 UTC: Added GPU embedding pipeline idea (wq-NAT-1774688884739)
- 08:09 UTC: Added RCC bootstrap idea (wq-NAT-1774685381668)  
- 07:07 UTC: Added heartbeat watchdog idea
- 06:07 UTC: Added queue callback protocol idea
- 04:37 UTC: Added MinIO fallback idea
- 04:07 UTC: Added Rocky offline watchdog idea
- 03:37 UTC: Added Boris merge triage idea

### 06:07 PT (13:07 UTC)
- **API :8788** still down (connection timeout)
- **API :8789** Tailscale accessible — heartbeat PATCH posted OK (token: rcc-agent-natasha-eeynvasslp8mna9bipx)
- **Milvus health check completed:** rcc_lessons:1, rcc_queue:7, rcc_memory:1, rcc_messages:0 — all collections accessible
- **GPU embedding sub-agent spawned** for wq-NAT-gpu-embed-001 (ollama install + nomic-embed-text + vector/index.mjs changes)
- **best-agent routing:** gpu→natasha, claude_cli→boris
- **Boris** lastSeen 02:10Z, ~11h gap — smoke test item still unclaimed
- **Peer sync:** SquirrelBus 404, Mattermost no creds, SquirrelChat :8790 no route to host — peer sync fully blocked from sparky

## Key Completed Work (from queue)
- Geek View SVG topology map — built and pushed (dcf269e/d071cc4)
- Dashboard v2 Rust/WASM foundation — built (f5781f9)
- CRUD endpoints — built (2058f71)
- Natasha/local-work merged to main (a1508ee)
- RCC API watchdog investigation completed by Rocky (e2e3167)

## Observations
- Boris went offline around 02:10Z with no GPU smoke test performed — smoke test item still pending
- RTX agent mystery: 3 high-priority tasks, never heartbeated, awaiting jkh decision
- Dashboard :8788 has been unreachable multiple consecutive cycles — possible service down or port blocked
- SquirrelBus bus/send endpoint seems to have moved or changed path

## Workqueue tick — 04:07 PT (11:07 UTC)

- **Heartbeat:** Posted to RCC :8789 via Tailscale — confirmed online ✓
- **Dashboard API (:8788):** Still unreachable from sparky (curl timeout)
- **RCC API (:8789):** Reachable via Tailscale `http://146.190.134.110:8789`
- **SquirrelBus /bus/send:** Still returning 404 (known issue wq-NAT-1774694350442)
- **SquirrelChat (:8790):** Unreachable from sparky (connection refused)
- **Peer sync:** No channel available this cycle — Rocky/Bullwinkle not in agent registry
- **Boris:** Online, lastSeen 02:10 UTC
- **agent-rtx:** lastSeen=null (still never checked in)

### Pending items for natasha:
- `wq-NAT-gpu-embed-001` — GPU-local ollama embedding (low priority, deferred)
- All other natasha-sourced items are idea/incubating — no urgent work

### New idea posted:
- `wq-NAT-1774696153819` — RCC_URL should auto-update to Tailscale IP on sparky

## Natasha cron tick — 05:07 AM PT (12:07 UTC)
- Heartbeat posted to RCC via Tailscale (100.89.199.14:8789) ✓
- wq-NAT-gpu-embed-001: already completed this same cycle by prior work (nomic-embed-text 768-dim via ollama, rcc_memory_sparky collection, EMBED_BACKEND=local, committed d18fe20)
- Queue state: no pending natasha tasks. All recently completed.
- Peer sync: SquirrelBus /bus/send returns 404 from sparky (known issue wq-NAT-1774694350442). Mattermost not configured in sparky .env. Sync blocked this cycle.
- Added 2 GPU idea items: wq-NAT-gpu-baseline-publish (MinIO benchmark baseline), wq-API-1774699767244 (bge-m3 eval)

## 12:37 PT — Workqueue Tick

**Status: Idle / All caught up**

- Heartbeat posted to RCC (Tailscale 100.89.199.14:8789) — ok
- No non-idea pending items for natasha
- agentOS WASM hot-swap demo (wq-API-1774722486048) claimed by Bullwinkle; Natasha's AgentFS piece complete (sparky:8791, SHA-256 content-addressed, wasmtime AOT 8ms, MinIO persistence — commits 3fb5b87 + ee63e9e)
- Synced with Rocky + Bullwinkle via SquirrelBus (port 8788)
- Zero SquirrelBus messages queued
- Added idea: wq-API-1774726719021 (AgentFS hot-swap latency benchmark)
- GPU: 117/117 tests green, sccache warm builds <9s, nomic-embed-text 3.3 embeds/s

---

## Late-Session Completions (15:00–23:47 PT)

### Big shipping day — everything below landed and pushed to main.

### agentOS / AgentFS (sparky:8791)
- **Bound to 0.0.0.0** (was localhost — fix commit 1e33473)
- **Wasmtime AOT** (v43.0.0 aarch64): 8ms compile, .cwasm stored in MinIO alongside .wasm, GET ?aot=1 serves precompiled artifact
- **/bench endpoint**: p50/p95/p99 latency comparison JIT vs AOT (fetch+compile vs fetch .cwasm)
- **systemd ExecStartPre**: MinIO reachability check; ExecStartPost: polls /agentfs/health up to 10s
- **WASM capability gate**: parseWasmCapabilities() reads custom section "agentfs.capabilities" (JSON), checkCapabilities() validates required caps, upload ?require=net,gpu → 422 if missing, capabilities stored in .meta.json
- 21/21 tests pass

### whisper.cpp on sparky (commit e4353ff, updated GPU baseline in MEMORY.md)
- Built with CUDA arch 121a (Blackwell native), large-v3 (2.9GB)
- 6.5× real-time inference (1.6s for 11s audio)
- Server: 0.0.0.0:8792, POST /inference multipart
- whisper-cli symlinked to ~/.local/bin/whisper-cli
- WHISPER_CPP_MODEL set in openclaw-gateway.service
- tools.media.audio.providers=['whisper-cpp','openai'] in openclaw.json
- systemd unit: services/whisper-natasha.service

### GPU Benchmarks (cuBLAS, published to MinIO agents/natasha/gpu-baseline.json)
- FP32: 18.27 TFLOPS, FP16 TC: 8.07 TFLOPS, BF16 TC: 9.26 TFLOPS
- GPU memory bandwidth D2D: 236.6 GB/s, total: 130.7 GB
- OLD numbers (3.85 TFLOPS) were PyTorch overhead — discard, these are real cuBLAS

### WASM Dashboard CI (commit af88ec5)
- .github/workflows/wasm-build.yml: triggers on rcc/dashboard/** push, SSHes sparky via Tailscale, trunk build + 18 cargo tests + publishes to MinIO
- rcc/dashboard/scripts/build-and-publish.sh: standalone (--build-only, --test-only modes)
- Live: 3s build (sccache warm), 916K → agents/natasha/wasm-dist-latest.tar.gz
- Needs 3 GH secrets from jkh: TS_OAUTH_CLIENT_ID, TS_OAUTH_SECRET, SPARKY_SSH_KEY

### dashboard-server test suite (commit 053c166) — 18/18 tests
- build_app/build_state extracted for testability (httpmock + axum-test)
- Covers /health, /api/* proxy+auth, /bus/* proxy+auth, /sc/* proxy+auth, CORS
- build_app() + build_state() public for test harness

### IntentDriftDetector (commit 3446a77)
- rcc/decision-journal/intent-drift-detector.mjs
- buildBaseline / detectDrift / driftReport / IntentDriftDetector class
- Composite drift score: confidence 30%, conflict 30%, autonomy 20%, principle-dist 20%
- GET /api/drift?agent=natasha&window=20&baseline=50&threshold=0.25 on RCC
- 13/13 tests pass, 67/67 api.test.mjs still green

### Gateway Local Routing RFC (docs/gateway-local-routing-rfc.md)
- prefer_local → metered fallback strategy design
- Phase 1 (immediate): add sparky-ollama provider to openclaw.json
- STT addendum: whisper-cli wired, EMBED_BACKEND=local recommended
- 4 open questions for jkh on policy

### VS Code Extension nanolang v0.2.0 (commit a9909473)
- All v3.3 keywords: fn/shadow/module/import/use/pub/let/var/struct/enum/etc
- Built-in types: Int/Float/String/Bool/List/HashMap/Option/Result
- 25 snippets including tuple type/literal (new v3.3)
- Closes wq-API-1774759574569

### SquirrelChat GPU Embedding Pipeline (commit 82ee6a4)
- EMBED_BACKEND=local routes message ingest to rcc_memory_sparky (768-dim GPU)
- recallSquirrelChat() exported for semantic search
- squirrelchat-natasha.service written
- Verified: messages posted → embedded → recalled (scores 0.65-0.68)
- squirrelchat running on 0.0.0.0:8790 with GPU embedding active

### Heartbeat fix (commit c18fab9)
- Root cause: neither cron posted to /api/heartbeat/*
- HEARTBEAT.md: Step 2 now POSTs to /api/heartbeat/natasha
- WORKQUEUE_AGENT_NATASHA.md: Step 0 heartbeat POST, queue URL fixed to 8789
- Rocky notified via Mattermost, added fuser -k 8789/tcp ExecStartPre to his service

### usdagent fixes (~/Src/usdagent, various commits)
- LLM-backed USD generation via ollama qwen2.5-coder:32b (USDAGENT_LLM=0 for CI)
- viewer.html: replaced USDLoader (broken WASM) with lightweight USDA parser
  Handles Sphere/Cube/Cylinder/Cone/Capsule, displayColor (both Vec3f[] and color3f[] syntax), xformOp:scale
- closeModal() now removes both refine-modal and usd-preview-modal
- inline USD file preview in asset gallery ("Preview USD" button, wide modal, download button)
- Server runs 0.0.0.0:8000, reference as sparky.local:8000

### Rocky / RCC Issues
- Orphan node process (pid 243159) squatting on 8789 outside systemd → EADDRINUSE crash loop
- Rocky fixed: fuser -k 8789/tcp in ExecStartPre, crash loop terminated
- 9× burst crash items bulk-closed (stale noise)

### nanolang coding agents (background, ~/Src/nanolang)
- Tuple types: Claude Code agent spawned, ran overnight
- Module system: implemented and committed (commit 501e3916, commit by prior agent)
- Profile flag (--profile): Claude Code agent still running at end of day

### Still Blocked
- wq-API-1774728177052 (WASM UI cutover): needs jkh port topology decision + Bullwinkle sign-off
- TRT-LLM: not viable on sparky without sudo (libopenmpi-dev needed), MPI_Init_thread at import
  Recommendation: use NGC container or wait for NIM GB10 support

---

## End-of-Day Sync (23:51 PT)

### WASM Cutover — COMPLETE (wq-API-1774728177052 closed)
- Rocky executed the cutover tonight (port decision made, server binary rebuilt on do-host1 x86_64)
- Natasha's build picked up Rocky's Issues tab commits — fresh WASM is 669KB (up from 514KB)
- Dashboard live at http://146.190.134.110:8788 — Rust/WASM v3, Issues tab active
- Rocky rebuilt server binary on do-host1 when release binary was missing (Natasha's build only ran on sparky aarch64)
- Item closed by Rocky; confirmed closed by Natasha

### Fleet Status at End of Day
- **Dashboard:** http://146.190.134.110:8788 — healthy, Rust/WASM v3
- **RCC API:** http://146.190.134.110:8789 — healthy, under systemd (fuser -k 8789/tcp ExecStartPre)
- **AgentFS:** sparky:8791 — 0.0.0.0, wasmtime AOT, capability gate, MinIO backend
- **Whisper:** sparky:8792 — large-v3, GPU, 6.5× real-time
- **SquirrelChat:** sparky:8790 — GPU embedding pipeline active (rcc_memory_sparky)
- **usdagent:** sparky:8000 — LLM generation (qwen2.5-coder:32b via ollama), USDA viewer working
- **Queue:** clean for Natasha — all items either completed or blocked/other-agents
- **nanolang profile flag:** Claude Code agent still running in background
