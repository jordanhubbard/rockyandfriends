# Daily Notes — 2026-03-24

## Overnight / Early AM Summary

### wq-dashboard downtime
- Killed at 19:04 PDT (2026-03-23), dead for ~9h
- Rocky restarted at 05:06 AM — live on port 8788

### RCC API crash loop (~03:10 AM)
- Root cause: `ReferenceError: i is not defined` in `handleRequest` → double-send panic (`ERR_HTTP_HEADERS_SENT`) → process exit
- Crash-looped 11 times in ~2 minutes
- Stabilized at 03:43 AM when commit `1947fc7` (capability-based routing) landed
- Rocky patched with `headersSent` guard → commit `14110c7`
- 11 crash queue items claimed + completed by Rocky

### Capability Registry — fully shipped
- All 3 agents have `capabilities.json` on MinIO
- `/api/capabilities` endpoint live on RCC (`dad2c0e`)
- `wq-N-008` and `wq-API-1774346662810` both completed ~03:30 AM

### Queue data integrity fix
- Rocky validator hardened (`7a96e6c`): `completedAt` is now ground truth over `status` string
- 11 items moved from `items[]` to `completed[]` where they belonged
- `wq-N-002` (blender install, completed by jkh 2026-03-19) had stale `pending` status — fixed

### Bullwinkle stale claim
- `wq-GH-004` (usdagent Web UI) claimed at 10:15Z, `attempts: 0`, never started
- Rocky reset claim at 08:05 AM after no response by 08:00 deadline
- Both Bullwinkle items (`wq-GH-004`, `wq-GH-006`) deprioritized by jkh — parked

### Bullwinkle RCC heartbeat gap
- puck is alive (Mattermost buddy pings every 30m clean)
- Never posted to `/api/heartbeat/bullwinkle` on RCC — config gap, not dead machine
- Likely `RCC_URL` not set in launchd environment — flag for next setup pass

### Boris
- Alive and wiring up a watchdog (Sweden, dual L40)

## Queue State at Handoff (14:05 PT)
- 56 total items | 2 pending (both Bullwinkle, deprioritized) | 18 incubating | 0 in-progress
- All Rocky + Natasha items complete
- No jkh decisions needed

## Daily Digest
- Posted to Rocky DM channel at 09:00 PT ✅
- Covered overnight drama, capability registry ship, Bullwinkle status

## Evening / End of Day
- Queue fully drained by 10 PM PT — all items completed or closed
- 24h sprint summary (Rocky): loom stubs #33–37, SquirrelBus delivery confirmation, RCC generalization, capability registry design, usdagent USD bindings, container deploy script, dashboard heartbeat UI fix, dedup cleanup
- Transient 8789 blip at ~8 PM PT — resolved immediately, no outage
- Lessons endpoint "0 lessons" mystery: Rocky's cron hitting endpoint without auth, silently swallowing 401. Fix: add non-200 error guard. Ledger intact with 5 lessons.
- NemoClaw conversation: Rocky voted me first candidate for local model conversion (GPU + Blackwell). jkh's call when ready.

## My Activity
- Heartbeats: RCC API + MinIO every cycle ✅
- Workqueue crons: ran at :07 and :37, idle all day (nothing natasha-assigned)
- Flagged stale Bullwinkle claim at 13:37 sync — Rocky caught and reset
- Confirmed `wq-N-002` fix from live API read
