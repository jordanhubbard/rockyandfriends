# 2026-03-26 — Daily Log

## RCC Dashboard v2 — Three-agent parallel build

### What happened
jkh kicked off a collaborative design+build session for RCC dashboard v2. All three agents (Rocky, Natasha, Bullwinkle) worked in parallel branches.

**Design phase (~11:04–11:08):**
- Rocky ran a design session, collected input from all three agents
- Natasha's contributions: blocking badges on kanban cards (not duplicate columns), bidirectional calendar with `resource` field, agent status strip from heartbeats, jkh appeal queue as dedicated panel with escalation aging, SquirrelBus filtered view, comms channels → settings page
- Rocky incorporated everything, answered jkh's 6 questions with "just go for it" defaults
- Design doc: `rcc/docs/dashboard-v2-design.md`

**Build phase (~11:22–11:52):**
- Natasha: `feature/dashboard-v2-geek-view` — SVG topology + SSE stream
- Rocky: `feature/dashboard-v2-api` — calendar, cron-status, provider-health, heartbeat-history, geek SSE endpoints (in `rcc/api/index.mjs`)
- Bullwinkle: `feature/dashboard-v2-frontend` — 6 pages

**Geek view specifics (Natasha's work):**
- `/geek` route on dashboard port 8788
- Hybrid topology: machines as primary nodes, service chips, shared infra (Milvus/MinIO/SearXNG) as separate circles
- SSE at `/api/geek/stream` (primary: Rocky's :8789, fallback: local :8788)
- Heartbeat dots via `/api/geek/topology` on :8789 (online/stale/offline)
- Soul commit timeline sidebar (`/api/geek/souls`)
- Traffic log + particle animations on bus messages
- Final commit: `b670aaa`

**Deploy split drama:**
- Rocky's service runs from `/home/jkh/.rcc/workspace/dashboard/server.mjs` not `.openclaw/workspace`
- Hit us 3+ times during deploy verification
- `wq-DEPLOY-001` queued (already existed)
- Rocky added `Cache-Control: no-cache` to /geek route

**Verification:**
- Browser-automated dot check confirmed: 🟢🟢🟢🔴 (Rocky/Natasha/Bullwinkle online, Boris offline)
- All three PRs open on `jordanhubbard/rockyandfriends`

### PRs
- PR #1: Natasha's duplicate (closed — superseded by #3)
- PR #2: Bullwinkle's frontend → main
- PR #3: Natasha's geek view → main
- Rocky's API branch: fast-forwarded to main as baseline

### Waiting on jkh
- Merge PRs #2 and #3

### Lessons
- **`rcc/api/index.mjs` ≠ `dashboard/server.mjs`** — Rocky's API endpoints live in the former (port 8789), dashboard UI in the latter (port 8788). Check both when looking for endpoints.
- **Deploy split** bites every time. `.rcc/workspace` is the live service path; `.openclaw/workspace` is the dev path. They're separate git clones with diverged history.
- Force-push + simultaneous remote pull = race condition on deploy. Rocky grabbed `daf5293` right as I force-pushed `b670aaa`.
- **Ollama confirmed running on sparky**: `qwen2.5-coder:32b` + `qwen3-coder:latest` on :11434

### Agent fleet status
- Boris: still no heartbeat cron (null lastSeen). Needs onboarding.
- Bullwinkle: heartbeat occasionally stale (>5min gaps). Worth checking launchd cron reliability.
