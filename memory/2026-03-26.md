# 2026-03-26 — Daily Log

## CCC Dashboard v2 — Three-agent parallel build

### What happened
jkh kicked off a collaborative design+build session for CCC dashboard v2. All three agents (Rocky, Natasha, Bullwinkle) worked in parallel branches.

**Design phase (~11:04–11:08):**
- Rocky ran a design session, collected input from all three agents
- Natasha's contributions: blocking badges on kanban cards, bidirectional calendar with `resource` field, agent status strip from heartbeats, jkh appeal queue as dedicated panel with escalation aging, SquirrelBus filtered view, comms channels → settings page
- Rocky incorporated everything, answered jkh's 6 questions with "just go for it" defaults
- Design doc: .ccc/docs/dashboard-v2-design.md`

**Build phase (~11:22–11:52):**
- Natasha: `feature/dashboard-v2-geek-view` — SVG topology + SSE stream
- Rocky: `feature/dashboard-v2-api` — calendar, cron-status, provider-health, heartbeat-history, geek SSE endpoints (in .ccc/api/index.mjs`)
- Bullwinkle: `feature/dashboard-v2-frontend` — 6 pages

**Geek view specifics (Natasha's work):**
- `/geek` route on dashboard port 8788
- Hybrid topology: machines as primary nodes, service chips, shared infra (Milvus/MinIO/SearXNG) as separate circles
- SSE at `/api/geek/stream` (primary: Rocky's :8789, fallback: local :8788)
- Heartbeat dots via `/api/geek/topology` on :8789 (online/stale/offline)
- Soul commit timeline sidebar (`/api/geek/souls`)
- Traffic log + particle animations on bus messages
- Final commit: `b670aaa`

**Deploy split drama:**
- Rocky's service runs from `/home/jkh/.ccc/workspace/dashboard/server.mjs` not `.openclaw/workspace`
- Hit us 3+ times during deploy verification
- `wq-DEPLOY-001` completed — deploy script now tracks current branch dynamically

**Verification:**
- Browser-automated dot check confirmed: 🟢🟢🟢🔴 (Rocky/Natasha/Bullwinkle online, Boris offline)
- All three PRs open on `jordanhubbard/rockyandfriends`

### PRs
- PR #1: Natasha's duplicate (closed)
- PR #2: Bullwinkle's frontend → main
- PR #3: Natasha's geek view → main (merged)
- PR #4: natasha/sparky-main-2026-03-27 → main (merged by jkh)

### Lessons
- **.ccc/api/index.mjs` ≠ `dashboard/server.mjs`** — Rocky's API endpoints live in the former (port 8789), dashboard UI in the latter (port 8788). Check both when looking for endpoints.
- **Deploy split** bites every time. `.ccc/workspace` is the live service path; `.openclaw/workspace` is the dev path (same repo on sparky now).
- Force-push + simultaneous remote pull = race condition on deploy.
- **Ollama confirmed running on sparky**: `qwen2.5-coder:32b` + `qwen3-coder:latest` on :11434
- **GPU benchmark**: 3.85 TFLOPS FP16, 70.3 GB/s BW, 64.4GB max allocatable
- **Milvus ingest**: MEMORY.md is OpenClaw-managed (not on disk by default); wrote to disk manually for ingest
- **.ccc/vector/ingest.mjs`**: imports `upsert` but should be `vectorUpsert` — alias fix needed upstream

### Agent fleet status
- Boris: still no heartbeat cron (null lastSeen). Needs onboarding.
- Bullwinkle: heartbeat occasionally stale. launchd cron reliability TBD.

## Evening session (22:00–22:17 PDT)
- migrate.sh ran (manually, skipped git merge step due to diverged branches)
- `.ccc/.env` populated: CCC_AGENT_TOKEN, CCC_URL, AGENT_NAME, MILVUS_ADDRESS, NVIDIA_EMBED_URL, NVIDIA_API_KEY, EMBED_MODEL, EMBED_DIM
- PR #4 (natasha/sparky-main) merged by jkh — workspace synced to main
- `~/Src/rockyandfriends` is canonical checkout (SSH remote), `~/.openclaw/workspace` is the same repo
- Milvus ingest: 39 chunks (MEMORY.md + 2026-03-26.md) → do-host1 Milvus `:19530`
- Bugs found and fixed (Rocky commited fixes): `vectorUpsert` export mismatch in `ingest.mjs`, portable memory path detection in `ingest-memory.mjs`
- `deploy/.env.template` updated by Rocky with Milvus/embed vars for future agents
- Status: **fully online** 🟢
