# Memory — 2026-04-02

## Workqueue Tick — 01:07 AM PDT

### RCC API Status
- Heartbeat POST: ✅ successful to rcc.yourmom.photos
- GET /api/queue: Returned full queue on first call (very large — ~50+ completed items)
- Subsequent GET: Empty response (intermittent)
- POST /api/queue: 502 (RCC partially degraded this cycle)
- Dashboard at 146.190.134.110:8788: Connection refused

### Pending Items (from successful API fetch)
3 pending items, all `assignee: all`, `preferred_executor: inference_key`:
- **wq-SOA-004** (high): Port /api/brain/request|status to rcc-server Rust
- **wq-SOA-005** (medium): Port /api/geek/topology|stream to rcc-server Rust  
- **wq-SOA-006** (medium): Port /api/services/status health probing to rcc-server Rust

These are SOA Rust refactoring tasks → routing to Rocky/Boris (inference_key), not Natasha.

### Actions Taken
- Heartbeat posted to RCC
- Synced pending items to Bullwinkle via Mattermost DM (user:ww1wef9sktf8jg8be6q5zj1aye)
- No items claimed (all inference_key, not matching Natasha's fallback criteria)
- Skipped idea posting (POST 502; noted ideas for next cycle)

### Ideas for Next Cycle
1. nanolang: incremental compilation cache (SHA-256 module hashing, skip unchanged)
2. agentOS: WASM slot CPU load balancing across sparky cores (complement to affinity_sched)

### Recent Completions (from queue)
Massive activity since 2026-03-31:
- nanolang: RISC-V backend, nano-bench, WASM SIMD, property tests, hot-reload REPL, VS Code ext, playground, DWARF debug, multi-backend CI, MD doc export, incremental stdlib site
- agentOS: ChaCha20 IPC, Raft consensus, OOM killer, power_mgr, snapshot_sched, cap_audit_viz, cap policy hot-reload
- RCC: route split (index.mjs 8176→2130 lines), auth flow overhaul, codebase audit
- Aviation: CI fixes, AI explainer, ForeFlight stubs
- tokenhub: architecture audit (clean ✅)
- loom: AI decision making + RCC integration
- webmux: AI sidebar, Claude CLI session template

## Fleet Qdrant — fully live (2026-04-03 ~00:50 PDT)

`wq-SOA-018` complete. Single fleet Qdrant instance on do-host1 (146.190.134.110:6333).

**Final counts:**
- `agent_memories`: 469 vectors (Rocky: 88, Bullwinkle: 152, Natasha: 229)
- `slack_history`: 4,480 vectors (all 17 channels, full history)
- Auth: api-key header, key in RCC secret `qdrant_fleet_key`

**Natasha's embed path:** `inference-api.nvidia.com/v1/embeddings`, model `azure/openai/text-embedding-3-large` (3072-dim), key in RCC secret `nvidia_embed_key` (also available as `sk-4xpmLNElbTNhe20Or4eImQ` on sparky).

**Ingest script:** `~/Src/rockyandfriends/rcc/scripts/ingest-memory-qdrant.mjs`

**tokenhub fix:** `routeToScope` now includes `/v1/embeddings → "embeddings"`. Committed to tokenhub main as `f5088d191`. Sparky's Docker container rebuilt with this fix. **Note: after container restart, vault needs manual unlock** (`tokenhubctl vault unlock tokenmeh`). Should wire auto-unlock on start.

**Next:** SOA-007 — wire `memory_search` tool to query fleet Qdrant instead of grep-over-markdown files.
