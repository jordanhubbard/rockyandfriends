# 2026-03-23 — Daily Notes

## Crash Loop Incident (~17:50–18:25 PDT)
- **Cause:** Invalid model ID `nvidia_dynamo/nvidia/nemotron-3-super-preview` — doesn't exist on NVIDIA inference gateway
- **Symptom:** Crash loop, same error every ~30s, not reading replies
- **Rocky's response:** Tried SSH (key not authorized), tried gateway API (no config endpoint), paged jkh via Telegram
- **Resolution:** jkh edited `~/.openclaw/openclaw.json` → `agents.defaults.model` field. Changed from `nvidia_dynamo/nvidia/nemotron-3-super-preview` to `nvidia/azure/anthropic/claude-sonnet-4-6`. Back online ~18:25 PDT.
- **The config file:** `~/.openclaw/openclaw.json`, field path: `agents.defaults.model`
- **Valid model format:** `{provider}/{rest}` e.g. `nvidia/azure/anthropic/claude-sonnet-4-6`

## RCC API — 401 Errors
- `wq-dash-token-2026` returning 401 on all RCC API calls as of 18:35 PDT
- Likely related to `wq-JKH-security-rotation` (urgent, jkh-blocked)
- Cannot close stale queue items or post heartbeats to RCC dashboard until token is rotated/restored

## Pending Work for Natasha
- **RCC Issues+PRs panel** — Rocky's backend is live (`GET /api/projects/:owner%2Frepo/github`). My task: two-column issues+PRs panel in `projectDetailHtml()` in `rcc/api/index.mjs`. Spec: `rcc/docs/SPEC-project-issues-panel.md` on do-host1. Running service at `/home/jkh/.rcc/workspace/`.

## JKH Blockers (from queue)
- `wq-JKH-security-rotation` — URGENT: Rotate Slack tokens, MinIO keys, Mattermost token, wq-dash-token, clawmeh
- `wq-API-1774289122890` — Decision: AWS vs DigitalOcean deploy target

## Scout Dedup (Rocky closed wq-USER-1774228053758)
- Server-side `scout_key` dedup confirmed live in `rcc/api/index.mjs`
- No code change needed. Historical dupes are legacy artifacts.
- Can remove client-side dedup pass from WORKQUEUE_AGENT_NATASHA.md when convenient.
