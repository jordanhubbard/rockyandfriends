# 2026-03-30 — Natasha Daily Log

## 00:00–01:32 AM Sprint

### nanolang stdlib Sprint 2 (commits 7a3f2b1 + fix a4c22e5)
- 12 HOFs: zip, any, all, flatten, flat_map, take, drop, enumerate, sum, product, sort_by, range
- 8 string utilities: str_repeat, str_index_of(+optional offset fix), str_pad_left, str_pad_right, str_reverse, chars, words, str_format
- 31 test assertions in test_stdlib_sprint2.nano — all pass
- Fixed regression: str_index_of(str, substr, offset) 3-arg form now works

### SquirrelChat threading polish (commit d9e4172)
- Thread replies no longer appear in main message list
- reply_count increments in real-time when WS thread reply arrives
- thread_unreads signal: tracks new replies since thread last opened
- New-reply badge: pulsing red ".sc-thread-new-badge" animation on thread button
- Active Threads sidebar section (shows ≤5 threaded messages, unread count)
- Clear unreads on thread open

### Voice Sprint 2 (commit from earlier)
- AgentVoiceConfig per agent (voice_id, speed, auto_play, speaker_label)
- default_voice_config(): nova/echo/alloy/shimmer for natasha/rocky/bullwinkle/boris
- Auto-play TTS on incoming WS messages when cfg.auto_play=true
- 🎙️ config panel in chat header
- play_audio_bytes via data URL

CCC: healthy. Queue: clean.

## 01:50–02:00 AM — stdlib typechecker fix

Root cause of test_stdlib_sprint2 failures:
- stdlib functions registered in stdlib.c (runtime eval env) AND typechecker.c (compile-time type env)
- My Sprint 2 functions were only in stdlib.c → E003 "Unknown function" at compile time
- Fix: added TC_BUILTIN() registrations for all 20 new functions in typechecker.c
- Lesson: every new stdlib function needs BOTH a runtime registration (stdlib.c) AND a typecheck registration (typechecker.c)

Commits: 7a3f2b1 (stdlib impl) → a4c22e5 (str_index_of offset fix) → typechecker fix (latest)
All tests pass: 15/15 unit, 4/4 integration, 6/6 lang, 177 userguide.
Clean clone + build confirmed on GitHub main.

## 02:03–02:10 AM — stdlib fix confirmed clean

Full resolution chain:
1. `test_stdlib_sprint2.nano` fails: functions only in stdlib.c, not typechecker.c → E003
2. Added TC_BUILTIN() → fixed E003 but 118 failures from strict arity check on variadic functions
3. Added TC_BUILTIN_VARIADIC() for str_index_of/str_pad_left/str_pad_right/str_format/range
4. Fresh clone + full suite: **202 pass, 0 fail** ✅

Stale sessions (quick-ro, fresh-ba, oceanic-) were running against intermediate commits
before the fixes landed — their failures are noise, not regressions.

nanolang main branch: HEAD = 8a1c0f6 (TC_BUILTIN_VARIADIC fix)
