name: Release

on:
  push:
    tags:
      - 'v*.*.*'

permissions:
  contents: write

jobs:
  release:
    name: Build and publish release artifacts
    runs-on: ubuntu-22.04

    steps:
      # ── Checkout ────────────────────────────────────────────────────────────
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # ── Node.js setup ───────────────────────────────────────────────────────
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: rcc-hub/package-lock.json

      # ── Install dependencies ─────────────────────────────────────────────────
      - name: Install rcc-hub dependencies
        working-directory: rcc-hub
        run: npm ci --omit=dev

      # ── Extract version from tag ─────────────────────────────────────────────
      - name: Extract version
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      # ── Build rcc-hub tarball ────────────────────────────────────────────────
      - name: Build rcc-hub tarball
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          ARCHIVE="rcc-hub-v${VERSION}.tar.gz"

          # Create a clean staging dir (no secrets, no .git)
          mkdir -p staging/rcc-hub
          rsync -a \
            --exclude='.git' \
            --exclude='.env' \
            --exclude='node_modules/.cache' \
            --exclude='data/' \
            --exclude='*.log' \
            rcc-hub/ staging/rcc-hub/

          # Write version into package.json
          node -e "
            const fs = require('fs');
            const p = 'staging/rcc-hub/package.json';
            const pkg = JSON.parse(fs.readFileSync(p));
            pkg.version = '${VERSION}';
            fs.writeFileSync(p, JSON.stringify(pkg, null, 2) + '\n');
          "

          tar -czf "${ARCHIVE}" -C staging rcc-hub
          sha256sum "${ARCHIVE}" > "${ARCHIVE}.sha256"
          echo "archive=${ARCHIVE}" >> "$GITHUB_OUTPUT"
          echo "Built ${ARCHIVE}"
        id: hub-build

      # ── Build rcc-agent tarball ──────────────────────────────────────────────
      - name: Build rcc-agent tarball
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          ARCHIVE="rcc-agent-v${VERSION}.tar.gz"

          # The agent installer is self-contained; bundle it with a README
          mkdir -p staging/rcc-agent
          cp rcc-hub/install-agent.sh staging/rcc-agent/
          cat > staging/rcc-agent/README.md << 'EOF'
          # CCC Agent Installer

          Run this on any machine you want to register as an agent:

              bash install-agent.sh

          It will prompt for the hub URL and a bootstrap token.
          EOF

          tar -czf "${ARCHIVE}" -C staging rcc-agent
          sha256sum "${ARCHIVE}" > "${ARCHIVE}.sha256"
          echo "archive=${ARCHIVE}" >> "$GITHUB_OUTPUT"
        id: agent-build

      # ── Extract changelog section ────────────────────────────────────────────
      - name: Extract release notes
        id: notes
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          if [ -f CHANGELOG.md ]; then
            # Extract section between this version header and the next
            awk "/^## \[?v?${VERSION}\]?/{found=1; next} found && /^## /{exit} found{print}" CHANGELOG.md > release-notes.md
          else
            echo "Release v${VERSION}" > release-notes.md
            echo "" >> release-notes.md
            echo "See commit history for changes." >> release-notes.md
          fi
          cat release-notes.md

      # ── Create GitHub Release ────────────────────────────────────────────────
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "v${{ steps.version.outputs.version }}"
          body_path: release-notes.md
          draft: false
          prerelease: ${{ contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') }}
          files: |
            rcc-hub-v${{ steps.version.outputs.version }}.tar.gz
            rcc-hub-v${{ steps.version.outputs.version }}.tar.gz.sha256
            rcc-agent-v${{ steps.version.outputs.version }}.tar.gz
            rcc-agent-v${{ steps.version.outputs.version }}.tar.gz.sha256

      # ── Update install scripts to point at versioned artifact ───────────────
      - name: Verify install scripts reference pinnable URLs
        run: |
          VERSION="${{ steps.version.outputs.version }}"
          echo "Release v${VERSION} published."
          echo "Install scripts should be updated to reference:"
          echo "  https://github.com/jordanhubbard/rockyandfriends/releases/download/v${VERSION}/rcc-hub-v${VERSION}.tar.gz"
          echo "  https://github.com/jordanhubbard/rockyandfriends/releases/download/v${VERSION}/rcc-agent-v${VERSION}.tar.gz"
