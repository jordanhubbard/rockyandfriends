{
  "name": "@agentos/migrate",
  "version": "0.1.0",
  "description": "CCC live WASM slot migration between mesh peers",
  "type": "module",
  "main": "index.mjs",
  "scripts": {
    "start": "node index.mjs",
    "test": "node --test tests/"
  },
  "engines": {
    "node": ">=22"
  }
}
